# トレンドキーワード自動抽出・分析システム 実装計画 (Docker & shadcn/ui & UX強化版)

要件定義・仕様書に基づき、X（Yahoo!リアルタイム検索）とnoteのトレンドデータを収集・突合・分析し、ダッシュボードで可視化するシステムを構築します。

本システムは、外部APIや巨大なローカルLLMを使用せず、ローカル環境で動作する軽量なNLPモデル（Sentence Transformers）を用いてキーワードの関連度を判定します。
フロントエンドUIには **`shadcn/ui` (React + TypeScript + Tailwind CSS)** を採用し、極めてプレミアムで洗練されたデザインと、ユーザーが直感的に操作できる管理ダッシュボードを構築します。

開発・実行環境は **Docker (Docker Compose)** を使用します。進捗管理は `docs/task.md` にて随時更新します。

## 1. 技術スタック
- **コンテナ仮想化**: `Docker`, `Docker Compose`
- **バックエンド（データ収集・処理・API）**: Python
  - ベースイメージ: `mcr.microsoft.com/playwright/python:v1.40.0-jammy` (Playwright公式イメージ)
  - データベース: `sqlite3` (設定、バッチステータス、およびnoteハッシュタグ統計の保存用)
  - ドメイン判定: `sentence-transformers` (`intfloat/multilingual-e5-small` を使用)
  - Bot対策: `playwright-stealth` (ヘッドレスブラウザシグネチャ偽装)
  - HTTPクライアント: `httpx` (Qiita/Zenn/note APIへの非同期リクエスト)
  - HTMLパーサー: `beautifulsoup4` (RSS/HTMLコンテンツのパース)
  - APIサーバー: `FastAPI` (非同期バックグラウンドタスク処理)
- **フロントエンド（ダッシュボード・設定UI）**: Node.js + Vite (React + TypeScript + Tailwind CSS)
  - UIコンポーネント: `shadcn/ui` (Radix UI)
  - グラフ描画: `Recharts` (React用チャートライブラリ)

## 2. データベース設計
SQLiteに以下のテーブルを作成します。
- `x_trends` (X/Yahooトレンド履歴)
- `note_ranking` (note上位記事データ)
- `note_hashtags` (noteハッシュタグの累積投稿件数および関連タグ情報)
- `keyword_analysis` (突合・スコア結果)
- `settings` (システム設定情報) - シードワード、ターゲットテーマ、NGワード、しきい値、noteカテゴリの保存用
- `batch_status` (バッチ実行進捗ステータス) - `status` (実行状態), `message` (進捗ログ), `progress` (％), `updated_at` (タイムスタンプ)

## 3. 作成するファイル構成
```text
reserch-workflows/
├── docker-compose.yml       # コンテナ全体の定義
├── backend/
│   ├── Dockerfile           # バックエンド用イメージ定義
│   ├── requirements.txt     # 依存ライブラリ
│   ├── db.py                # データベース操作 (settings/batch_status/note_hashtags/supply_count対応)
│   ├── scraper.py           # X/note/Qiita/Zenn/Brain/Tipsのデータ収集 (playwright-stealth対応)
│   ├── local_filter.py      # NLPモデルによる類似度フィルタリング (動的ネガティブ生成対応)
│   ├── analyzer.py          # スコアリング・4象限分類・Priority Score算出
│   └── main.py              # FastAPIサーバー (ハッシュタグ統計・クロスメディアAPI対応)
├── frontend/
│   ├── Dockerfile           # フロントエンド用イメージ定義
│   ├── package.json         # React/Tailwind/shadcn依存関係
│   ├── tailwind.config.js   # Tailwind設定
│   ├── vite.config.ts       # Vite設定 (APIプロキシ設定)
│   ├── components.json      # shadcn/ui設定
│   ├── src/
│   │   ├── main.tsx         # エントリポイント
│   │   ├── App.tsx          # メインレイアウト (Tabs制御、「ハッシュタグ調査」タブ統合)
│   │   ├── index.css        # Tailwind/shadcn用グローバルスタイル
│   │   ├── components/      # UIコンポーネント
│   │   │   ├── Dashboard.tsx      # 4象限(Recharts)・キーワードテーブル・プログレス
│   │   │   ├── HashtagResearch.tsx # noteハッシュタグ統計ボリューム表示＆可視化
│   │   │   ├── Settings.tsx       # 設定フォーム・類似度テストシミュレータ
│   │   │   └── ui/                # shadcn/ui自動生成コンポーネント群
│   │   └── lib/
│   │       └── utils.ts     # shadcn/ui用ユーティリティ
└── docs/
    ├── トレンドキーワード自動抽出・分析システム 要件定義・仕様書.md
    ├── トレンドキーワード自動抽出・分析システム 実装計画書.md (本ファイル)
    ├── トレンドキーワード自動抽出・分析システム ユーザーマニュアル.md
    └── task.md              # タスクリスト (実装中に随時更新)
```

## 4. 追加仕様（UX・アルゴリズム向上策）
1. **APIプロキシ機能**: フロントエンドからのAPIリクエスト (`/api/*`) を、Vite開発サーバー経由で直接バックエンドコンテナにプロキシし、CORS問題の解消とコードのポータビリティを担保します。
2. **バッチ処理進捗の可視化**: 収集バッチ実行中、収集フェーズごとにステータス（例: "Yahooトレンド収集中...", "NLP判定中 (10/30)..."）と進捗パーセンテージをDBに記録し、UI上のプログレスバーにリアルタイム反映します。
3. **グラフ・テーブル相互インタラクション**: 4象限グラフ上の任意の点（キーワード）をクリックすると、詳細データテーブルの該当レコードに自動でスクロール＆ハイライトします。
4. **類似度テストツール（シミュレーター）**: 設定画面にテキストを入力して「テスト」をクリックすると、現在のテーマとのコサイン類似度スコアおよびネガティブテーマとの相対的な比較を行い、判定結果を返す機能を搭載し、しきい値調整を容易にします。
5. **note先行・X話題性突合アプローチ**: 実需のあるnoteランキングキーワードおよびシードハッシュタグ関連タグを先行取得し、その後にX上での話題性（ポスト数）を個別に突合します。これにより、短期的なXのノイズワードを効率的に排除します。
6. **ハッシュタグ需要調査画面**: 収集したシードハッシュタグの累積投稿件数と関連ハッシュタグを一覧表示し、CSSベースのプレミアム棒グラフでシェアを視覚化します。

## 5. 実装プロセス
1. **Dockerインフラ構成とDB実装**:
   - `Dockerfile`, `docker-compose.yml` の作成。
   - `backend/db.py` の実装とテーブルの初期化。
2. **スクレイピングとフィルタリングの実装**:
   - `backend/scraper.py`, `backend/local_filter.py` の実装。
3. **データ分析とAPIの実装**:
   - `backend/analyzer.py`, `backend/main.py` の実装（進捗・シミュレータAPIを含む）。
4. **フロントエンドの土台構築と設定**:
   - Vite + React + TSプロジェクト構成、Tailwind CSS/shadcn/ui/Rechartsの初期化とインストール。
5. **ダッシュボードと設定UIの実装**:
   - Rechartsグラフ連携、プログレスバー、設定管理、類似度シミュレータの実装。
6. **動作検証**:
   - 全体ビルド起動テストと、UI操作における各統合機能の確認。
7. **プラットフォーム機能の拡張とアプローチ変更**:
   - noteハッシュタグ統計機能の追加（`note_hashtags` テーブルの作成、API連携）。
   - 「ハッシュタグ調査」画面の実装（noteとQiitaのクロスメディア比較表示を含む）。
   - note先行・X突合アプローチへのアルゴリズム変更とスコアリングの適正化。

8. **改善実装（Playwrightステルス・実用性強化版）**:
   - `playwright-stealth` の導入とフィンガープリント偽装ロジックの統合。
   - Qiita/Zenn/Brain/Tipsのトレンドデータ収集スクレイパーの追加。
   - 時事ニュースRSSからの動的ネガティブテーマ自動生成（`local_filter.py` 拡張）。
   - 供給量調査（Qiita API）と Priority Score 算出アルゴリズムの実装（`db.py` スキーマ拡張、`analyzer.py` 拡張）。
   - ダッシュボードへの「ブルーオーシャン推奨ランキング」ウィジェット追加、テーブルに「供給数」「優先度」列を追加。
